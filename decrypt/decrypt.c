#include "decrypt.h"

unsigned char sbox[256] = 
{
	
};

unsigned char inv_sbox[256] = 
{
	
};

unsigned char matrix_mix_inv[] = {
	0x0e, 0x0b, 0x0d, 0x09,
	0x09, 0x0e, 0x0b, 0x0d,
	0x0d, 0x09, 0x0e, 0x0b,
	0x0b, 0x0d, 0x09, 0x0e
};

void Search(char * path, DEC_DATA * data)
{
	DIR * dir;
	struct dirent * ent;

	if(!(dir = opendir(path)))
	{
		printf("open %s error \n", path);
		return;
	}
		
	printf("%s \n", path);

	while((ent = readdir(dir))!=NULL)
	{
		if(ent->d_type == DT_DIR)
		{
			char dirname[255];

			if(ent->d_name[0] == '.')
			{
				continue;
			}
			else
			{
				strcpy(dirname, path);
				strcat(dirname, ent->d_name);
				strcat(dirname, "/");
				Search(dirname, data);
			}
		}
		else if(ent->d_type == DT_REG)
		{
			const char * nameptr = &(ent->d_name[strlen(ent->d_name)-4]);
			if((strcmp(nameptr, ".enc"))==0)
			{
				char file_encrypt[255];
				char file_decrypt[255];

				strcpy(file_encrypt, path);
				strcat(file_encrypt, ent->d_name);

				strcpy(file_decrypt, path);
				strcat(file_decrypt, ent->d_name);
				memset(&(file_decrypt[strlen(file_decrypt)-4]), 0, 4);

				FILE * fpin;
				FILE * fpout;

				if(!(fpin = fopen(file_encrypt, "r")))
				{
					printf("read %s open error \n", file_encrypt);
					continue;
				}	

				if(!(fpout = fopen(file_decrypt, "w")))
				{
					printf("write %s open error \n", file_decrypt);
					continue;
				}

				Decryption_CBC(data, fpin, fpout);

				remove(file_encrypt);

				fclose(fpin);
				fclose(fpout);
			}
		}
	}
	char readme[255];
	strcpy(readme, path);
	strcat(readme, "ReadMe");

	remove(readme);
	closedir(dir);
}

int FileSize(FILE * fp)
{
	int filesize;

	fseek(fp, 0L, SEEK_END);
	filesize = ftell(fp);
	fseek(fp, 0L, SEEK_SET);

	return filesize;
}

void Padding(unsigned char buf[], FILE * fpout)
{
	int padnum = buf[BLOCK-1];
	fwrite(buf, sizeof(unsigned char), BLOCK-padnum, fpout);
}

void InitDecryption(DEC_DATA * data, DATA ikey[], unsigned char vec[])
{
	MakeRcon(data->rcon);
	KeyExpansion(data->key, data->rcon, ikey);
	memcpy(data->vec, vec, BLOCK);
}

void Decryption_CBC(DEC_DATA * data, FILE * fpin, FILE * fpout)
{
	int readsize = 0;
	int filesize = FileSize(fpin);
	unsigned char buf[BLOCK];
	unsigned char vec[BLOCK];
	unsigned char vec_temp[BLOCK];

	memcpy(vec, data->vec, BLOCK);

	while(1)
	{
		readsize += fread(buf, sizeof(unsigned char), BLOCK, fpin);
		memcpy(vec_temp, buf, BLOCK);
		Decrypt(data->key, buf);
		CBC(vec, buf);
		if(readsize >= filesize)
		{
			Padding(buf, fpout);
			break;
		}
		memcpy(vec, vec_temp, BLOCK);
		fwrite(buf, sizeof(unsigned char), BLOCK, fpout);
	}
}

void CBC(unsigned char vector[], unsigned char text[])
{
	int i;

	for(i=0; i<BLOCK; i++)
	{
		text[i] ^= vector[i];
	}
}

void Decrypt(DATA * key, unsigned char cipher[])
{
	int i;

	AddRoundKey(key, cipher, 14); 
		
	for(i=13; i>0; i--)
	{
		InvShiftRows(cipher);
		InvSubBytes(cipher);
		AddRoundKey(key, cipher, i);
		InvMixColumns(cipher);
	}

	InvShiftRows(cipher);
	InvSubBytes(cipher);
	AddRoundKey(key, cipher, 0);	
}

void InvMixColumns(unsigned char cipher[])
{
	int i, j;
	unsigned char temp[4];

	for(i=0; i<Nb; i++)
	{
		for(j=0; j<4; j++)
		{
			temp[j] = Multiplication(matrix_mix_inv[j*Nb], cipher[i*Nb]);
			temp[j] = Addition(temp[j], Multiplication(matrix_mix_inv[j*Nb+1], cipher[i*Nb+1]));
			temp[j] = Addition(temp[j], Multiplication(matrix_mix_inv[j*Nb+2], cipher[i*Nb+2]));
			temp[j] = Addition(temp[j], Multiplication(matrix_mix_inv[j*Nb+3], cipher[i*Nb+3]));
		}
		for(j=0; j<4; j++)
		{
			cipher[i*Nb+j] = temp[j];
		}
	}
}

void InvShiftRows(unsigned char cipher[])
{
	int i, j;
	unsigned char temp;

	for(i=1; i<4; i++)
	{
		for(j=i; j>0; j--)
		{
			temp = cipher[i+Nb*3];
			cipher[i+Nb*3] = cipher[i+Nb*2];
			cipher[i+Nb*2] = cipher[i+Nb*1];
			cipher[i+Nb*1] = cipher[i];
			cipher[i] = temp;
		}
	}
}
	
void InvSubBytes(unsigned char cipher[])
{
	int i;

	for(i=0; i<Nb; i++)
	{
		InvSubWord((DATA*) &cipher[i*4]);
	}
}

void AddRoundKey(DATA * key, unsigned char cipher[], int idx)
{
	int i, j;

	for(i=0; i<Nb; i++)
	{
		for(j=0; j<4; j++)
		{
			cipher[4*i+j] = key[idx*4+i].arr[j] ^ cipher[4*i+j];
		}
	}
}

unsigned char Addition(int n1, int n2)
{
	return (unsigned char)(n1^n2);
}	

unsigned char Multiplication(int n1, int n2)
{
	int res = 0x0;

	while(1)
	{
		if(n2 % 2) // 나머지가 1일 경우 xor (초기값부터 포함하기 위해 제일 먼저 검사)
			res ^= n1;

		if((n2 /= 2) == 0) // n2가 2로 더이상 나누어지지 않을 때까지
		{
			break;
		}
		else
		{
			/* n1에 2곱하고 8bit를 넘어서면 xor 11b(x^8+x^4+x^3+x^1+x^0)를 해준다. */
			/* 그리고 그 값을 n1에 대입한다. */
			n1 *= 2;
			if(n1 > 0xff)
			{	
				n1 ^= 0x11b;
			}
		}
	}

	return (unsigned char)res;
}

unsigned char GenerateRconNum(int num)
{
	int shift = num-1;
	int res = 1;
	int delta = num-8;

	if(shift < 8)
		return (res << shift);
	else
	{	
		res = res << 7;
		
		// shift 할 때 마다 8bit넘는지 검사 후 넘을 시 xor 11b를 해준다.
		while(1)
		{
			if(delta > 0)
				delta--;
			else
				break;
			
			res = res << 1;
			if(res > 0xff)
			{
				res ^= 0x11b;
			}
		}
	}

	return (unsigned char)res;
}

void MakeRcon(DATA * rcon)
{
	int i;

	for(i=0; i<Nk-1; i++)
	{
		rcon[i].arr[0] = GenerateRconNum(i+1);
		rcon[i].arr[1] = 0x0;
		rcon[i].arr[2] = 0x0;
		rcon[i].arr[3] = 0x0;
	}
}

void RotWord(DATA * data)
{
	unsigned char temp;
	temp = data->arr[0];

	data->arr[0] = data->arr[1];
	data->arr[1] = data->arr[2];
	data->arr[2] = data->arr[3];
	data->arr[3] = temp;
}

void SubWord(DATA * data)
{
	data->arr[0] = sbox[data->arr[0]];
	data->arr[1] = sbox[data->arr[1]];
	data->arr[2] = sbox[data->arr[2]];
	data->arr[3] = sbox[data->arr[3]];
}

void InvSubWord(DATA * data)
{
	data->arr[0] = inv_sbox[data->arr[0]];
	data->arr[1] = inv_sbox[data->arr[1]];
	data->arr[2] = inv_sbox[data->arr[2]];
	data->arr[3] = inv_sbox[data->arr[3]];
}

void KeyExpansion(DATA * key, DATA * rcon, DATA * ikey)
{
	int i;
	
	// ikey값 8열을 key의 첫 8열에 입력
	for(i=0; i<Nk; i++)
	{
		key[i] = ikey[i];
	}
	
	// 나머지 keySchedule
	for(i=1; i<Nk; i++)
	{
		MakeKey(key, rcon, i);
	}	
}

void MakeKey(DATA * key, DATA * rcon, int idx)
{
	int i, j;
	DATA temp;
	
	if(idx == 7) // 마지막에는 4열만 생성하므로
	{
		for(i=0; i<Nb; i++)
		{
			temp = key[idx*Nk-1+i];

			if(i % Nk == 0)
			{
				RotWord(&temp);
				SubWord(&temp);
				for(j=0; j<4; j++)
				{
					temp.arr[j] = temp.arr[j] ^ rcon[idx-1].arr[j];
				}
			}

			for(j=0; j<4; j++)
			{
				temp.arr[j] = key[(idx-1)*Nk+i].arr[j] ^ temp.arr[j];
			}

			key[idx*Nk+i] = temp;	
		}
	}
	else
	{
		for(i=0; i<Nk; i++)
		{
			temp = key[idx*Nk-1+i];

			switch(i % Nk)
			{
				case 0 :
					RotWord(&temp);
					SubWord(&temp);
					for(j=0; j<4; j++)
					{
						temp.arr[j] = temp.arr[j] ^ rcon[idx-1].arr[j];
					}
					break;

				case 4 :
					SubWord(&temp);
					break; 
			}

			for(j=0; j<4; j++)
			{
				temp.arr[j] = key[(idx-1)*Nk+i].arr[j] ^ temp.arr[j];
			}

			key[idx*Nk+i] = temp;	
		}
	}
}
